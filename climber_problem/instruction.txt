本文档是对爬山问题算法的说明（heshengjie2011@163.com）：

算法基本思路：
	定义两个数组，一个数组step用来表示该点最大的数值，通过遍历input指针内容可以获得；另外一个数组flag用来表示这个点是否和后面的
点有连接，定义这个数组是便于计算，在计算总和的时候是依据两点数据相加还是相减；若点i和前面一个点相连，即flag[i-1]=1,则
sum += abs(step[i]-step[i-1]) + 1;否则 sum += step[i]+step[i-1] + 1;

时间复杂度：O(n)
空间复杂度：O(n)
